//write a function that takes a function, and makes it callable with two invocations

const extensions = (func) => {
    // let count = 0;
    // count++;
    // if(count < 3){
    //     if(typeof cb !== 'function' && typeof num !== 'number'){
    //         throw new Error();
    //     }
    // }    
    // return func();
    // };

    console.log(extensions);
};

module.exports = { extensions };
